# 工作日志 · 2026-08-28

## 日期
- 日期：2026-08-28
- 项目：C4F7N 便携式检漏仪（屏幕 v18 + STM32F103RCT6 固件）

## 今日目标
完成 MCU 固件阶段 3（标定 / W25Q16 存储 / 单位切换）并构建通过，随后系统化整理项目进度与文档导航。

## 完成事项

### 1. 阶段 3 固件实现（核心开发）
| 模块 | 文件 | 内容 |
|---|---|---|
| W25Q16 驱动 | `w25q16.c/h` | SPI1（SCK=PA5/MISO=PA6/MOSI=PA7/CS=PA4），读/写/擦扇区/页编程，Mode0 9MHz |
| 参数持久化 | `param_store.c/h` | 标定系数+高/低阈值+蜂鸣器+单位模式存 0 扇区，CRC 校验，上电 `param_init` 加载 |
| 标定回调 | `main.c` | 零点标定 `b=−k·V/1000`、量程标定 `k=(1000−b)·1000/V`、恢复出厂 |
| 单位换算 | `main.c` | ppm↔g/y（×0.458）按 unit_mode 显示 |
| 阈值回读 | `main.c`+`hmi_get.c` | 屏幕 `get` 应答解析后写回 W25Q16，区分 hi/lo 归属 |

**构建结果**：`C4F7N_Demo.hex` 编译链接通过（19200B 级 ROM / 6.2% RAM）。

### 2. 阶段 3 成果归档
- 接口文档：新增 §8「传感器+标定+存储接口」。
- 接入教程：第 5 步改为 W25Q16 落库并补「存储说明」。
- 生成映射表：新增 §5.8「MCU 固件阶段 1/2/3 交付」。
- project memory：新增 Stage 1-3 Deliverables。

### 3. 项目进度整理（product-lifecycle-workbench 插件）
- 用 internal-memo 规范产出一份状态更新备忘录。
- 新建项目根 `C4F7N_LeakDetector/README.md`（含进度段），并同步 `C4F7N_Demo/README.md`。

### 4. 硬件连接表整理
- 依据固件代码逐一核对引脚，产出屏幕/传感器/W25Q16/烧录四张连接表。
- 写入接口文档 §8.6，并入 DOCS.md §0 速查。

### 5. 文档导航体系
- 新建项目根 `DOCS.md`：按「目标 → 一键跳转」组织，覆盖全部 27 个 markdown 文档。
- `DOCS.md` 回链加入全部 6 个 README，形成互相导航。
- 更新 `DOCS.md` §0 反映最新硬件连接表。

## 交付物清单
- 固件：`C4F7N_Demo/MDK-ARM/build/C4F7N_Demo.hex`
- 文档：接口文档、接入教程、生成映射表、项目根 README、固件 README、DOCS.md、本日志

## 遗留项
| 项 | 状态 |
|---|---|
| 历史曲线波形回灌（`hmi_on_range`） | 未实现，留空 |
| 导出 CSV（USB CDC，`hmi_on_export`） | 占位 |
| 真机烧录联调 | 待硬件（ST-Link + 板子上电 + 屏幕下载 v18） |

## 下一步
1. 真机联调：ST-Link（SWD）烧录 `C4F7N_Demo.hex` + 屏幕下载 `C4F7N_HMI_v18.HMI`，双向验证。
2. 确定曲线页数据来源后实现 `hmi_on_range`。